//===> class: FilterPage
import 'package:flutter/material.dart';

class FilterPage extends StatelessWidget{
  @override
  Widget build(BuildContext context){
      return Scaffold(
        appBar: AppBar(
          title: Text("FilterPage", style: TextStyle(color: Colors.black),),
        ),
        body:Container(),);
  }//ef
}//ec
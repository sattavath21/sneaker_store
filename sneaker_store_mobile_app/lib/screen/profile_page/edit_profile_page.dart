//===> class: undefined
import 'package:flutter/material.dart';

class EditProfilePage extends StatelessWidget{
  @override
  Widget build(BuildContext context){
      return Scaffold(
        appBar: AppBar(
          title: Text("EditProfilePage", style: TextStyle(color: Colors.black),),
        ),
        body:Container(),);
  }//ef
}//ec
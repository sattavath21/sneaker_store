
<div id="app1">
    <div class="text-h5 text-weight-bold text-white q-pa-lg"
     style="background-color: #ff3333; color: rgb(230, 229, 229); text-color: white;
            border-top-left-radius: 10px; /* Adjust the radius as needed */
            border-top-right-radius: 10px; /* Adjust the radius as needed */">
  Delivery Information
</div>


    <q-table class="bg-grey-2" title="Supplier Information" :rows="suppliers" :columns="columns" :filter="searchText">
        
        <template v-slot:top-left>
            <div class="text-h6 q-pa-md">Supplier Details</div>
            <q-separator vertical class="q-my-sm"></q-separator>
            <div class="q-pl-sm">
                <q-input debounce="200" dense v-model="searchText" type="text">
                    <template v-slot:prepend>
                        <q-icon name="search"></q-icon>
                    </template>
                </q-input>
            </div>
        </template>
        <template v-slot:body="props">
            <q-tr :props="props">
                <q-td key="index" :props="props">{{ suppliers.indexOf(props.row) + 1 }}</q-td>
                <q-td key="supplierName" :props="props">
                    {{ props.row.supplierName }}
                    <q-popup-edit v-model="props.row.supplierName">
                        <q-input v-model="props.row.supplierName" dense autofocus counter></q-input>
                        <div class="justify-end row">
                            <q-btn color="blue-grey-6" @@click="editSupplier(props.row)" icon="edit" label="edit"
                                v-close-popup="cancelEnabled">
                            </q-btn>
                        </div>
                    </q-popup-edit>
                </q-td>
                <q-td key="supplierPhone" :props="props">
                    {{ props.row.supplierPhone }}
                    <q-popup-edit v-model="props.row.supplierPhone">
                        <q-input v-model="props.row.supplierPhone" dense autofocus counter></q-input>
                        <div class="justify-end row">
                            <q-btn color="blue-grey-6" @@click="editSupplier(props.row)" icon="edit" label="edit"
                                v-close-popup="cancelEnabled">
                            </q-btn>
                        </div>
                    </q-popup-edit>
                </q-td>
                <q-td key="supplierEmail" :props="props">
                    {{ props.row.supplierEmail }}
                    <q-popup-edit v-model="props.row.supplierEmail">
                        <q-input v-model="props.row.supplierEmail" dense autofocus counter></q-input>
                        <div class="justify-end row">
                            <q-btn color="blue-grey-6" @@click="editSupplier(props.row)" icon="edit" label="edit"
                                v-close-popup="cancelEnabled">
                            </q-btn>
                        </div>
                    </q-popup-edit>
                </q-td>
                
                <q-td key="actions" :props="props">
                    <q-btn class="q-ma-sm q-px-lg q-py-sm" color="red-6" @@click="confirm = true" icon="backspace"></q-btn>
                    
                </q-td>
            </q-tr>
        </template>
        <q-dialog v-model="confirm" persistent>
                        <q-card>
                            <q-card-section class="row items-center">
                                <q-avatar icon="person_remove" color="red" text-color="white"></q-avatar>
                                <span class="q-ml-sm">Are you sure you want to delete this supplier?</span>
                            </q-card-section>

                            <q-card-actions align="center">
                                <q-btn class="q-ma-sm" color="red-6" @@click="deleteSupplier(props.row)" label="Yes" v-close-popup></q-btn>
                                <q-btn class="q-ma-sm" label="No" color="grey" v-close-popup />
                            </q-card-actions>
                        </q-card>
                    </q-dialog>
    </q-table>

    <div class=" bg-grey-9 q-pa-sm"></div>

    <q-table class="bg-grey-2" title="Address Information" :rows="suppliersAddress" :columns="columnsAddress" :filter="searchText2">
        <template v-slot:top-left>
            <div class="text-h6 q-pa-md">Address Details</div>
            <q-separator vertical class="q-my-sm"></q-separator>
            <div class="q-pl-sm">
                <q-input debounce="200" dense v-model="searchText2" type="text">
                    <template v-slot:prepend>
                        <q-icon name="search"></q-icon>
                    </template>
                </q-input>
            </div>
        </template>
        <template v-slot:body="props">
            <q-tr :props="props">
                <q-td key="index" :props="props">{{ suppliersAddress.indexOf(props.row) + 1 }}</q-td>
                <q-td key="no" :props="props">
                    {{ props.row.no }}
                    <q-popup-edit v-model="props.row.no">
                        <q-input v-model="props.row.no" dense autofocus counter></q-input>
                        <div class="justify-end row">
                            <q-btn color="blue-grey-6" @@click="editSupplierAddress(props.row)" icon="edit" label="edit"
                                v-close-popup="cancelEnabled">
                            </q-btn>
                        </div>
                    </q-popup-edit>
                </q-td>
                <q-td key="street" :props="props">
                    {{ props.row.street }}
                    <q-popup-edit v-model="props.row.street">
                        <q-input v-model="props.row.street" dense autofocus counter></q-input>
                        <div class="justify-end row">
                            <q-btn color="blue-grey-6" @@click="editSupplierAddress(props.row)" icon="edit" label="edit"
                                v-close-popup="cancelEnabled">
                            </q-btn>
                        </div>
                    </q-popup-edit>
                </q-td>
                <q-td key="amphur" :props="props">
                    {{ props.row.amphur }}
                    <q-popup-edit v-model="props.row.amphur">
                        <q-input v-model="props.row.amphur" dense autofocus counter></q-input>
                        <div class="justify-end row">
                            <q-btn color="blue-grey-6" @@click="editSupplierAddress(props.row)" icon="edit" label="edit"
                                v-close-popup="cancelEnabled">
                            </q-btn>
                        </div>
                    </q-popup-edit>
                </q-td>
                <q-td key="province" :props="props">
                    {{ props.row.province }}
                    <q-popup-edit v-model="props.row.province">
                        <q-input v-model="props.row.province" dense autofocus counter></q-input>
                        <div class="justify-end row">
                            <q-btn color="blue-grey-6" @@click="editSupplierAddress(props.row)" icon="edit" label="edit"
                                v-close-popup="cancelEnabled">
                            </q-btn>
                        </div>
                    </q-popup-edit>
                </q-td>
                <q-td key="postalCode" :props="props">
                    {{ props.row.postalCode }}
                    <q-popup-edit v-model="props.row.postalCode">
                        <q-input v-model="props.row.postalCode" dense autofocus counter></q-input>
                        <div class="justify-end row">
                            <q-btn color="blue-grey-6" @@click="editSupplierAddress(props.row)" icon="edit" label="edit"
                                v-close-popup="cancelEnabled">
                            </q-btn>
                        </div>
                    </q-popup-edit>
                </q-td>
                <q-td key="actions" :props="props">
                    <q-btn class="q-ma-sm q-px-lg q-py-sm" color="red-6" @@click="confirm = true" icon="backspace"></q-btn>
                    <q-dialog v-model="confirm" persistent>
                        <q-card>
                            <q-card-section class="row items-center">
                                <q-avatar icon="person_remove" color="red" text-color="white"></q-avatar>
                                <span class="q-ml-sm">Are you sure you want to delete this supplier?</span>
                            </q-card-section>

                            <q-card-actions align="center">
                                <q-btn class="q-ma-sm" color="red-6" @@click="deleteSupplierAddress(props.row)" label="Yes" v-close-popup></q-btn>
                                <q-btn class="q-ma-sm" label="No" color="grey" v-close-popup />
                            </q-card-actions>
                        </q-card>
                    </q-dialog>
                </q-td>
            </q-tr>
        </template>
    </q-table>

    <q-btn class="q-my-md q-py-md" color="green" icon="add_box" label="Add Deliver Location" size="md"
        onclick='window.location="/Supplier/AddSup"'></q-btn>
</div>

@section code{
    <script>
    var option = {
        //data
        data(){
            return {
                confirm: false,
                searchText: "",
                searchText2: "",
                suppliers: []
                ,
                suppliersAddress: []
                ,
                columns:[
                    {
                        name: 'index',
                        field: 'index',
                        align: 'left',
                        sortable: true,
                        label: 'No'
                    },
                    {
                        name:  'supplierName',
                        label: 'Supplier Name',
                        field: 'supplierName',
                        sortable: true,
                        align: 'center'
                    },
                    {
                        name:  'supplierPhone',
                        label: 'Supplier Phone',
                        field: 'supplierPhone',
                        sortable: true,
                        align: 'center'
                    },
                    {
                        name:  'supplierEmail',
                        label: 'Supplier Email',
                        field: 'supplierEmail',
                        sortable: true,
                        align: 'center'
                    },
                     {
                        name:  'actions',
                        label: 'Actions',
                        field: 'actions',
                        align: 'center'
                    }
                ]
                ,
                columnsAddress: [
                        {
                            name: 'index',
                            field: 'index',
                            align: 'left',
                            sortable: true,
                            label: 'No'
                        },
                        {
                            name: 'no',
                            label: 'No',
                            field: 'no',
                            sortable: true,
                            align: 'center'
                        },
                        {
                            name: 'amphur',
                            label: 'Amphur',
                            field: 'amphur',
                            sortable: true,
                            align: 'center'
                        },
                        {
                            name: 'province',
                            label: 'Province',
                            field: 'province',
                            sortable: true,
                            align: 'center'
                        },
                        {
                            name: 'postalCode',
                            label: 'Postal Code',
                            field: 'postalCode',
                            sortable: true,
                            align: 'center'
                        },
                        {
                            name: 'actions',
                            label: 'Actions',
                            field: 'actions',
                            align: 'center'
                        }
                    ]
            };
        }
        ,
        //methods
        methods:{
            async editSupplier(item) {

                    var url = "api/EditSupplier";
                    try {
                        console.log(item);
                        var result = await axios.post(url, item);
                        console.log(result.data);
                    }
                    catch (error) {
                        console.log(error);
                    }

                }//ef
                ,
                async editSupplierAddress(item) {

                    var url = "api/EditSupplierAddress";
                    try {
                        console.log(item);
                        var result = await axios.post(url, item);
                        console.log(result.data);
                    }
                    catch (error) {
                        console.log(error);
                    }

                }//ef
                ,
                async deleteSupplier(item) {
                    console.log(item);
                    var url = "/api/DeleteSupplier";
                    var del = item
                    var result = await axios.post(url, del);
                    this.suppliers = this.suppliers.filter(x => x.supplierId != item.supplierId);
                    this.suppliersAddress = this.suppliersAddress.filter(x => x.supplierAddressId != item.supplierAddressId);
                    this.$q.notify({
                        type: 'negative',
                        color: "red-8",
                        message: 'Supplier had been deleted'
                    })
                }//ef
                ,
                async deleteSupplierAddress(item) {
                    console.log(item);
                    var url = "/api/DeleteSupplierAddress";
                    var del = item
                    var result = await axios.post(url, del);
                    this.suppliers = this.suppliers.filter(x => x.supplierAddressId != item.supplierAddressId);
                    this.suppliersAddress = this.suppliersAddress.filter(x => x.supplierAddressId != item.supplierAddressId);
                    this.$q.notify({
                        type: 'negative',
                        color: "red-8",
                        message: 'Supplier had been deleted'
                    })
                }//ef

        }
        ,
        //computed
        computed:{

        }
        ,
        //created
        async created(){
            try{
                    var response = await axios.get("/api/GetSuppliers");
                    this.suppliers = response.data;
            }
            catch(error){
                    console.log(error);
            }
            try{
                    var response = await axios.get("/api/GetSupplierAddress");
                    this.suppliersAddress = response.data;
            }
            catch(error){
                    console.log(error);
            }

        }//ecreated
        ,
        //mounted
        mounted(){
            
        }

    };
    
    const app = Vue.createApp(option);
    app.use(Quasar)
    app.mount('#app1')
    </script>
}